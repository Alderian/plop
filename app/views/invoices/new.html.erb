<h1>New invoice</h1>

<% form_for(@invoice,:builder => LabelFormBuilder) do |f| %>
  <%= f.error_messages %>

  <%= render :partial=>'form',:locals => {:f=>f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', invoices_path %>
